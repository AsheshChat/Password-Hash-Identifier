# Password-Hash-Identifier
Developed a Python tool to identify common password hash formats using   prefix, length, and character pattern analysis.Supports bcrypt, Argon2, PBKDF2, scrypt, Unix crypt formats, MySQL hashes,   and common raw hash algorithms.
